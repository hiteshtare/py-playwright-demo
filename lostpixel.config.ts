import type { CustomProjectConfig } from 'lost-pixel';

export const config: CustomProjectConfig = {
 customShots: {
    currentShotsPath: "./lost-pixel",
  },
  // OSS mode 
  // generateOnly: true,
  // failOnDifference: true
  
  // Lost Pixel Platform (to use in Platform mode, comment out the OSS mode and uncomment this part )
  lostPixelProjectId: "cmiei0utq0m4x60m5br286er9",
  apiKey: "765ebaff-d0a1-4153-8043-19f44a24c035",
};